package org.acme.quickstart;

import io.quarkus.test.junit.NativeImageTest;

@NativeImageTest
public class NativeReactiveRouteIT extends ReactiveRouteTest {

    // Execute the same tests but in native mode.
}